import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: '01catastro-browser',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
